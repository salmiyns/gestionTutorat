{% extends 'base.html.twig' %}

{% block title %}Tutore index{% endblock %}

{% block body %}
    <h1>Tutore index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tutore in tutores %}
            <tr>
                <td>{{ tutore.id }}</td>
                <td>
                    <a href="{{ path('tutore_show', {'id': tutore.id}) }}">show</a>
                    <a href="{{ path('tutore_edit', {'id': tutore.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('tutore_new') }}">Create new</a>
{% endblock %}
