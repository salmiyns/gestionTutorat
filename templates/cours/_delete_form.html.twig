
<form method="post" action="{{ path('cours_delete', {'id': cour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <a href="{{ path('cours_index') }}" class="mr-3"> <i class="fa fa-long-arrow-alt-left"></i>  Retour </a>
    <a href="#" class="mr-2 text-50"><b>Proposition</b></a>
    <a href="{{ path('cours_edit', {'id': cour.id}) }}" class="text-50" style="text-decoration: underline;">Modifier</a>
								

    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cour.id) }}">
    <button class="btn"> <i class="fa fa-trash"></i></button>
</form>
